doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        link(rel="icon", href="data:;base64,iVBORw0KGgo=")
        title Document
    body 
        h1 👋 채팅 시스템 실험 사이트입니다
        if loggedIn
            span 안녕하세요! #{loggedInUser.name}님
            ul
                li
                    form(method="POST" action="/logout")
                        input(type="submit" value="로그아웃하기")
            span 유저 목록 (가능한 채팅 목록)
            ul
            each user in allUsers
                if user.id != loggedInUser.id
                    li
                        a(href=`${user.id}`)=user.name
        else
            ul
                li
                    a(href="login") 로그인하기
                li
                    a(href="register") 회원가입하기
