extends base

block content 
    video(src="/" + video.fileUrl, controls, width="1300", height="500")
    div
        p=video.description
        small=video.createdAt
    a(href=`${video.id}/edit`) 🛠 Edit Video &rarr;
    br
    a(href=`${video.id}/delete`) ❌ Delete Video &rarr;