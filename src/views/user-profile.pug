extends base
include mixins/video

block content
    div.user_contents
        div.user_contents__logo
            img(src=`/${user.avatarUrl}`)
            span.user_contents__username #{user.username}
            if loggedInUser && (String(loggedInUser._id) == String(user.id))
                span.user_contents__edit
                    a(href="edit") Edit Profile
        div.user_contents__videos
            each video in user.videos 
                +videos(video)
            else 
                h4 🙅 Sorry nothing found.